# Suno Helper 웹 전환 설계 — "홈페이지 로그인 → 실행"

> 2026-09-04 설계. 목표: 사용자가 whick.org에 **로그인한 뒤에만** UI를 쓸 수 있고,
> 실제 작업(AI 생성·오디오 리마스터·유튜브 업로드)은 **각자의 컴퓨터에서** 실행된다.

---

## 1. 현재 상태 (이미 완성된 것)

| 구성 | 현황 |
|---|---|
| 로컬 백엔드 | FastAPI + SQLite, **API 102개** (`127.0.0.1:8765`) |
| 로컬 UI | React 19 + Vite (10개 페이지, `localhost:5173`) |
| AI 연동 | OpenRouter·OpenAI·Claude·Gemini — **API 키를 사용자 설정 화면에서 입력** (서버 공유 키 불필요) |
| 유튜브 업로드 | OAuth (client_secret·refresh_token을 로컬 DB settings에 저장) |
| 실행 | `start.bat` 원클릭 (venv 생성 → 백엔드+프론트 기동) |
| 저장 | 전부 로컬 (`data/` 폴더 — DB·업로드·편집 설정) |

**핵심 발견**: 프론트가 이미 `API_BASE = "/api"` (상대경로)라서,
**UI를 어디서 서빙하든 같은 출처의 `/api`로 호출**하는 구조. 전환 난이도를 크게 낮춘다.

---

## 2. 목표 구조

```
[사용자 브라우저]
  whick.org/suno-helper          ← 웹 UI (로그인 필요, CC 사이트 회원)
        │  fetch("/api/...")      ← 같은 출처 = CORS 문제 없음
        ▼
[로컬 프록시]  localhost:5173 (Suno Helper 데스크탑 서버)
        │
        ▼
[로컬 엔진]  FastAPI 102개 API 그대로
  AI 생성 · 오디오 리마스터 · 유튜브 업로드 · 파일 저장
  (전부 사용자 PC에서만 일어남 — 서버 부담 0)
```

- 웹 UI와 로컬 엔진의 통신은 **사용자 컴퓨터 내부**에서만 일어난다
  (브라우저 → `localhost:5173`). 인터넷으로 오디오·키가 오가지 않는다.
- whick.org는 **UI 제공 + 로그인 관문** 역할만 한다.

---

## 3. 설계 원칙 (보안)

| 항목 | 설계 | 이유 |
|---|---|---|
| UI 접근 | whick.org 계정 **로그인 필수** (기존 CC 사이트 회원·JWT 재사용) | 파파님 요구 — 가입 유도·관리 |
| 로컬 API 인증 | 로컬 프록시가 **로그인 세션 토큰 검증** 후 API 통과 | 다른 웹사이트가 사용자 로컬 엔진을 마음대로 호출하는 것 차단 |
| 오리진 | UI가 whick.org에서 서빙되면 `/api`는 같은 출처(CORS 0) | 브라우저 보안 정책상 가장 안전 |
| API 키 | 사용자가 자기 키를 로컬 엔진 설정에 입력 (현 구조 유지) | 키가 서버에 모이지 않음 |
| 데이터 | 전부 사용자 PC (`data/`) | 서버에 음원·영상 저장 안 됨 |
| 터널 | 로컬 엔진은 `127.0.0.1` 바인딩만 (현재 기본값) | 포트 노출 금지 규칙 준수 |

> 참고: 브라우저에서 `https://whick.org` 페이지가 `http://localhost:5173`을 호출하는 것은
> **mixed content 아님** (localhost는 예외 허용), 하지만 몇 가지 브라우저 정책(CORS)을
> 피하려면 **로컬 프록시가 UI도 같이 서빙**하는 방식(§5 방식 B)이 더 단순하다.

---

## 4. 전환 단계 (총 6단계)

### 1단계 — 정리·기반 (소규모)
- `.env`·`.env.bak` 삭제 (빈 값이지만 히스토리 오염 방지), `Suno Helper - Start.lnk` 등 Windows 전용 파일 정리
- git 브랜치 `web-mode` 생성, GitHub private repo 생성 후 push
- `.gitignore` 점검 (`data/`, `.env` 제외 확인 완료)

### 2단계 — 인증 게이트 (핵심)
- 백엔드(FastAPI)에 **간단한 토큰 검증 미들웨어** 추가
  - whick.org가 발급한 **사이트 JWT**(`site_oauth_token` 재사용)을 검사
  - UI 접속 시: `whick.org` 로그인 → 토큰 발급 → 로컬 엔진에 등록(한 번) → 이후 자동
  - 미인증 요청은 UI에서 "whick.org 로그인 필요" 안내로 유도
- whick.org 측: `/suno-helper` 경로는 **비로그인 시 auth.html로 리다이렉트**

### 3단계 — UI 서빙 방식 결정 (둘 중 택1)
- **방식 A — whick.org 서빙**: 프론트 빌드를 whick.org의 `/suno-helper/` 경로에 배포.
  UI의 `/api` 호출이 whick.org로 가는 문제 → **로컬 프록시 주소로 리다이렉트하는 서비스 워커** 또는
  Vite `base`/`API_BASE` 환경변수로 `http://localhost:8765` 직접 지정 (CORS 화이트리스트에 `https://whick.org` 추가)
- **방식 B — 로컬 서빙 (추천)**: 엔진이 UI도 서빙(이미 `frontend/dist` 서빙 코드 존재) +
  사용자는 `localhost:8765` 북마크로 사용. whick.org는 로그인·설명·엔진 다운로드·문서 역할.
  → 구현 최소·가장 안전. 단, "UI가 홈페이지에서 보인다"는 요구에는 A가 맞음
- **권장**: A(홈페이지 UI) + B(오프라인 대체) 병행. 1차는 A로 진행.

### 4단계 — 로컬 프록시·설치형 배포
- `start.bat` 개선: 첫 실행 시 **whick.org 로그인 → 토큰 붙여넣기(또는 자동 감지)** 마법사
- Windows 설치 프로그램(또는 휴대용 zip): Python·Node 내장 임베디드로 "설치 클릭 3번" 수준으로
- 업데이트: git tag 기반 셀프업데이트 스크립트

### 5단계 — whick.org 통합
- 홈페이지: 수노헬퍼 카드 "출시 예정" → "배포중" 전환, 다운로드 안내 페이지
- GitHub `sabzzilfafa-lang/whick` → `suno-helper/` 폴더에 소스 반영 (또는 별도 repo 링크)
- 사용자 매뉴얼(설치·로그인·사용 순서) 작성 — manual.html에 추가

### 6단계 — 검증·배포
- 로컬 전 기능 회귀 테스트 (AI 생성·편집기 8단계·유튜브 업로드)
- 미인증 접근 차단 테스트 (로컬 API 무토큰 호출 401)
- 다른 PC에서 설치 → 로그인 → 사용 E2E 테스트

---

## 5. 사용자 경험 (최종 모습)

1. 사용자가 `whick.org` 접속 → **회원가입/로그인** (기존 CC 계정)
2. 홈페이지 → 제품 → **Suno Helper** → "열기"
3. 엔진 미설치 PC면: **설치 안내 페이지** → 설치 프로그램 다운로드 → 실행
4. 엔진 설치 PC면: whick.org UI가 곧바로 열리고 각 단계 사용
   (가사·프롬프트 생성 → Suno 복사 → 업로드 → 리마스터 → 유튜브)
5. 모든 파일·키·유튜브 권한은 사용자 PC에만 존재

---

## 6. 리스크·대응

| 리스크 | 대응 |
|---|---|
| 브라우저가 localhost 호출 제한 (환경별) | 방식 B(로컬 서빙) 폴백 제공 |
| 유튜브 OAuth 리다이렉트 (로컬↔웹) | OAuth redirect를 `localhost:8765/callback`로 유지 — 현 구조 그대로 |
| 엔진 업데이트 분산 | 버전 체크 API + 셀프업데이트 |
| 무료 배포·로그인 운영 정책 | whick.org 이용약관에 "로컬 실행 소프트웨어" 조항 추가 |

---

## 7. 작업 소요 추정

| 단계 | 내용 | 추정 |
|---|---|---|
| 1 | 정리·GitHub | 0.5h |
| 2 | 인증 게이트 | 2~3h |
| 3 | UI 서빙(A) | 2~4h |
| 4 | 설치 프로그램 | 3~5h |
| 5 | 홈페이지 통합·매뉴얼 | 2h |
| 6 | 테스트 | 1~2h |
| 합계 | | **약 1~2일 작업** |
